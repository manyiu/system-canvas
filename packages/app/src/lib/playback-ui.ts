/** Show scenario chips when count is at or below this; otherwise use a select. */
export const SCENARIO_CHIP_THRESHOLD = 5;

/** Show step button bar when count is at or below this; otherwise scrubber + list. */
export const STEP_CHIP_THRESHOLD = 6;
